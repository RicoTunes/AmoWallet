# Railway deployment config
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --include=prod"]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
